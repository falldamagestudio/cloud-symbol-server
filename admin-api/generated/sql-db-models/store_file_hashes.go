// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// StoreFileHash is an object representing the database table.
type StoreFileHash struct {
	HashID int      `boil:"hash_id" json:"hash_id" toml:"hash_id" yaml:"hash_id"`
	FileID null.Int `boil:"file_id" json:"file_id,omitempty" toml:"file_id" yaml:"file_id,omitempty"`
	Hash   string   `boil:"hash" json:"hash" toml:"hash" yaml:"hash"`
	Status string   `boil:"status" json:"status" toml:"status" yaml:"status"`

	R *storeFileHashR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L storeFileHashL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var StoreFileHashColumns = struct {
	HashID string
	FileID string
	Hash   string
	Status string
}{
	HashID: "hash_id",
	FileID: "file_id",
	Hash:   "hash",
	Status: "status",
}

var StoreFileHashTableColumns = struct {
	HashID string
	FileID string
	Hash   string
	Status string
}{
	HashID: "store_file_hashes.hash_id",
	FileID: "store_file_hashes.file_id",
	Hash:   "store_file_hashes.hash",
	Status: "store_file_hashes.status",
}

// Generated where

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpernull_Int struct{ field string }

func (w whereHelpernull_Int) EQ(x null.Int) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Int) NEQ(x null.Int) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Int) LT(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Int) LTE(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Int) GT(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Int) GTE(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_Int) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_Int) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_Int) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Int) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var StoreFileHashWhere = struct {
	HashID whereHelperint
	FileID whereHelpernull_Int
	Hash   whereHelperstring
	Status whereHelperstring
}{
	HashID: whereHelperint{field: "\"cloud_symbol_server\".\"store_file_hashes\".\"hash_id\""},
	FileID: whereHelpernull_Int{field: "\"cloud_symbol_server\".\"store_file_hashes\".\"file_id\""},
	Hash:   whereHelperstring{field: "\"cloud_symbol_server\".\"store_file_hashes\".\"hash\""},
	Status: whereHelperstring{field: "\"cloud_symbol_server\".\"store_file_hashes\".\"status\""},
}

// StoreFileHashRels is where relationship names are stored.
var StoreFileHashRels = struct {
	File                 string
	HashStoreUploadFiles string
}{
	File:                 "File",
	HashStoreUploadFiles: "HashStoreUploadFiles",
}

// storeFileHashR is where relationships are stored.
type storeFileHashR struct {
	File                 *StoreFile           `boil:"File" json:"File" toml:"File" yaml:"File"`
	HashStoreUploadFiles StoreUploadFileSlice `boil:"HashStoreUploadFiles" json:"HashStoreUploadFiles" toml:"HashStoreUploadFiles" yaml:"HashStoreUploadFiles"`
}

// NewStruct creates a new relationship struct
func (*storeFileHashR) NewStruct() *storeFileHashR {
	return &storeFileHashR{}
}

func (r *storeFileHashR) GetFile() *StoreFile {
	if r == nil {
		return nil
	}
	return r.File
}

func (r *storeFileHashR) GetHashStoreUploadFiles() StoreUploadFileSlice {
	if r == nil {
		return nil
	}
	return r.HashStoreUploadFiles
}

// storeFileHashL is where Load methods for each relationship are stored.
type storeFileHashL struct{}

var (
	storeFileHashAllColumns            = []string{"hash_id", "file_id", "hash", "status"}
	storeFileHashColumnsWithoutDefault = []string{"hash", "status"}
	storeFileHashColumnsWithDefault    = []string{"hash_id", "file_id"}
	storeFileHashPrimaryKeyColumns     = []string{"hash_id"}
	storeFileHashGeneratedColumns      = []string{"hash_id"}
)

type (
	// StoreFileHashSlice is an alias for a slice of pointers to StoreFileHash.
	// This should almost always be used instead of []StoreFileHash.
	StoreFileHashSlice []*StoreFileHash
	// StoreFileHashHook is the signature for custom StoreFileHash hook methods
	StoreFileHashHook func(context.Context, boil.ContextExecutor, *StoreFileHash) error

	storeFileHashQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	storeFileHashType                 = reflect.TypeOf(&StoreFileHash{})
	storeFileHashMapping              = queries.MakeStructMapping(storeFileHashType)
	storeFileHashPrimaryKeyMapping, _ = queries.BindMapping(storeFileHashType, storeFileHashMapping, storeFileHashPrimaryKeyColumns)
	storeFileHashInsertCacheMut       sync.RWMutex
	storeFileHashInsertCache          = make(map[string]insertCache)
	storeFileHashUpdateCacheMut       sync.RWMutex
	storeFileHashUpdateCache          = make(map[string]updateCache)
	storeFileHashUpsertCacheMut       sync.RWMutex
	storeFileHashUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var storeFileHashAfterSelectHooks []StoreFileHashHook

var storeFileHashBeforeInsertHooks []StoreFileHashHook
var storeFileHashAfterInsertHooks []StoreFileHashHook

var storeFileHashBeforeUpdateHooks []StoreFileHashHook
var storeFileHashAfterUpdateHooks []StoreFileHashHook

var storeFileHashBeforeDeleteHooks []StoreFileHashHook
var storeFileHashAfterDeleteHooks []StoreFileHashHook

var storeFileHashBeforeUpsertHooks []StoreFileHashHook
var storeFileHashAfterUpsertHooks []StoreFileHashHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *StoreFileHash) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *StoreFileHash) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *StoreFileHash) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *StoreFileHash) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *StoreFileHash) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *StoreFileHash) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *StoreFileHash) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *StoreFileHash) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *StoreFileHash) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range storeFileHashAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddStoreFileHashHook registers your hook function for all future operations.
func AddStoreFileHashHook(hookPoint boil.HookPoint, storeFileHashHook StoreFileHashHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		storeFileHashAfterSelectHooks = append(storeFileHashAfterSelectHooks, storeFileHashHook)
	case boil.BeforeInsertHook:
		storeFileHashBeforeInsertHooks = append(storeFileHashBeforeInsertHooks, storeFileHashHook)
	case boil.AfterInsertHook:
		storeFileHashAfterInsertHooks = append(storeFileHashAfterInsertHooks, storeFileHashHook)
	case boil.BeforeUpdateHook:
		storeFileHashBeforeUpdateHooks = append(storeFileHashBeforeUpdateHooks, storeFileHashHook)
	case boil.AfterUpdateHook:
		storeFileHashAfterUpdateHooks = append(storeFileHashAfterUpdateHooks, storeFileHashHook)
	case boil.BeforeDeleteHook:
		storeFileHashBeforeDeleteHooks = append(storeFileHashBeforeDeleteHooks, storeFileHashHook)
	case boil.AfterDeleteHook:
		storeFileHashAfterDeleteHooks = append(storeFileHashAfterDeleteHooks, storeFileHashHook)
	case boil.BeforeUpsertHook:
		storeFileHashBeforeUpsertHooks = append(storeFileHashBeforeUpsertHooks, storeFileHashHook)
	case boil.AfterUpsertHook:
		storeFileHashAfterUpsertHooks = append(storeFileHashAfterUpsertHooks, storeFileHashHook)
	}
}

// One returns a single storeFileHash record from the query.
func (q storeFileHashQuery) One(ctx context.Context, exec boil.ContextExecutor) (*StoreFileHash, error) {
	o := &StoreFileHash{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for store_file_hashes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all StoreFileHash records from the query.
func (q storeFileHashQuery) All(ctx context.Context, exec boil.ContextExecutor) (StoreFileHashSlice, error) {
	var o []*StoreFileHash

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to StoreFileHash slice")
	}

	if len(storeFileHashAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all StoreFileHash records in the query.
func (q storeFileHashQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count store_file_hashes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q storeFileHashQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if store_file_hashes exists")
	}

	return count > 0, nil
}

// File pointed to by the foreign key.
func (o *StoreFileHash) File(mods ...qm.QueryMod) storeFileQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"file_id\" = ?", o.FileID),
	}

	queryMods = append(queryMods, mods...)

	return StoreFiles(queryMods...)
}

// HashStoreUploadFiles retrieves all the store_upload_file's StoreUploadFiles with an executor via hash_id column.
func (o *StoreFileHash) HashStoreUploadFiles(mods ...qm.QueryMod) storeUploadFileQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"cloud_symbol_server\".\"store_upload_files\".\"hash_id\"=?", o.HashID),
	)

	return StoreUploadFiles(queryMods...)
}

// LoadFile allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (storeFileHashL) LoadFile(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStoreFileHash interface{}, mods queries.Applicator) error {
	var slice []*StoreFileHash
	var object *StoreFileHash

	if singular {
		var ok bool
		object, ok = maybeStoreFileHash.(*StoreFileHash)
		if !ok {
			object = new(StoreFileHash)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStoreFileHash)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStoreFileHash))
			}
		}
	} else {
		s, ok := maybeStoreFileHash.(*[]*StoreFileHash)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStoreFileHash)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStoreFileHash))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &storeFileHashR{}
		}
		if !queries.IsNil(object.FileID) {
			args = append(args, object.FileID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &storeFileHashR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.FileID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.FileID) {
				args = append(args, obj.FileID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`cloud_symbol_server.store_files`),
		qm.WhereIn(`cloud_symbol_server.store_files.file_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load StoreFile")
	}

	var resultSlice []*StoreFile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice StoreFile")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for store_files")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for store_files")
	}

	if len(storeFileHashAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.File = foreign
		if foreign.R == nil {
			foreign.R = &storeFileR{}
		}
		foreign.R.FileStoreFileHashes = append(foreign.R.FileStoreFileHashes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.FileID, foreign.FileID) {
				local.R.File = foreign
				if foreign.R == nil {
					foreign.R = &storeFileR{}
				}
				foreign.R.FileStoreFileHashes = append(foreign.R.FileStoreFileHashes, local)
				break
			}
		}
	}

	return nil
}

// LoadHashStoreUploadFiles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (storeFileHashL) LoadHashStoreUploadFiles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStoreFileHash interface{}, mods queries.Applicator) error {
	var slice []*StoreFileHash
	var object *StoreFileHash

	if singular {
		var ok bool
		object, ok = maybeStoreFileHash.(*StoreFileHash)
		if !ok {
			object = new(StoreFileHash)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStoreFileHash)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStoreFileHash))
			}
		}
	} else {
		s, ok := maybeStoreFileHash.(*[]*StoreFileHash)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStoreFileHash)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStoreFileHash))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &storeFileHashR{}
		}
		args = append(args, object.HashID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &storeFileHashR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.HashID) {
					continue Outer
				}
			}

			args = append(args, obj.HashID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`cloud_symbol_server.store_upload_files`),
		qm.WhereIn(`cloud_symbol_server.store_upload_files.hash_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load store_upload_files")
	}

	var resultSlice []*StoreUploadFile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice store_upload_files")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on store_upload_files")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for store_upload_files")
	}

	if len(storeUploadFileAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.HashStoreUploadFiles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &storeUploadFileR{}
			}
			foreign.R.Hash = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.HashID, foreign.HashID) {
				local.R.HashStoreUploadFiles = append(local.R.HashStoreUploadFiles, foreign)
				if foreign.R == nil {
					foreign.R = &storeUploadFileR{}
				}
				foreign.R.Hash = local
				break
			}
		}
	}

	return nil
}

// SetFile of the storeFileHash to the related item.
// Sets o.R.File to related.
// Adds o to related.R.FileStoreFileHashes.
func (o *StoreFileHash) SetFile(ctx context.Context, exec boil.ContextExecutor, insert bool, related *StoreFile) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"cloud_symbol_server\".\"store_file_hashes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"file_id"}),
		strmangle.WhereClause("\"", "\"", 2, storeFileHashPrimaryKeyColumns),
	)
	values := []interface{}{related.FileID, o.HashID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.FileID, related.FileID)
	if o.R == nil {
		o.R = &storeFileHashR{
			File: related,
		}
	} else {
		o.R.File = related
	}

	if related.R == nil {
		related.R = &storeFileR{
			FileStoreFileHashes: StoreFileHashSlice{o},
		}
	} else {
		related.R.FileStoreFileHashes = append(related.R.FileStoreFileHashes, o)
	}

	return nil
}

// RemoveFile relationship.
// Sets o.R.File to nil.
// Removes o from all passed in related items' relationships struct.
func (o *StoreFileHash) RemoveFile(ctx context.Context, exec boil.ContextExecutor, related *StoreFile) error {
	var err error

	queries.SetScanner(&o.FileID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("file_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.File = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.FileStoreFileHashes {
		if queries.Equal(o.FileID, ri.FileID) {
			continue
		}

		ln := len(related.R.FileStoreFileHashes)
		if ln > 1 && i < ln-1 {
			related.R.FileStoreFileHashes[i] = related.R.FileStoreFileHashes[ln-1]
		}
		related.R.FileStoreFileHashes = related.R.FileStoreFileHashes[:ln-1]
		break
	}
	return nil
}

// AddHashStoreUploadFiles adds the given related objects to the existing relationships
// of the store_file_hash, optionally inserting them as new records.
// Appends related to o.R.HashStoreUploadFiles.
// Sets related.R.Hash appropriately.
func (o *StoreFileHash) AddHashStoreUploadFiles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*StoreUploadFile) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.HashID, o.HashID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"cloud_symbol_server\".\"store_upload_files\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"hash_id"}),
				strmangle.WhereClause("\"", "\"", 2, storeUploadFilePrimaryKeyColumns),
			)
			values := []interface{}{o.HashID, rel.FileID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.HashID, o.HashID)
		}
	}

	if o.R == nil {
		o.R = &storeFileHashR{
			HashStoreUploadFiles: related,
		}
	} else {
		o.R.HashStoreUploadFiles = append(o.R.HashStoreUploadFiles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &storeUploadFileR{
				Hash: o,
			}
		} else {
			rel.R.Hash = o
		}
	}
	return nil
}

// SetHashStoreUploadFiles removes all previously related items of the
// store_file_hash replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Hash's HashStoreUploadFiles accordingly.
// Replaces o.R.HashStoreUploadFiles with related.
// Sets related.R.Hash's HashStoreUploadFiles accordingly.
func (o *StoreFileHash) SetHashStoreUploadFiles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*StoreUploadFile) error {
	query := "update \"cloud_symbol_server\".\"store_upload_files\" set \"hash_id\" = null where \"hash_id\" = $1"
	values := []interface{}{o.HashID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.HashStoreUploadFiles {
			queries.SetScanner(&rel.HashID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Hash = nil
		}
		o.R.HashStoreUploadFiles = nil
	}

	return o.AddHashStoreUploadFiles(ctx, exec, insert, related...)
}

// RemoveHashStoreUploadFiles relationships from objects passed in.
// Removes related items from R.HashStoreUploadFiles (uses pointer comparison, removal does not keep order)
// Sets related.R.Hash.
func (o *StoreFileHash) RemoveHashStoreUploadFiles(ctx context.Context, exec boil.ContextExecutor, related ...*StoreUploadFile) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.HashID, nil)
		if rel.R != nil {
			rel.R.Hash = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("hash_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.HashStoreUploadFiles {
			if rel != ri {
				continue
			}

			ln := len(o.R.HashStoreUploadFiles)
			if ln > 1 && i < ln-1 {
				o.R.HashStoreUploadFiles[i] = o.R.HashStoreUploadFiles[ln-1]
			}
			o.R.HashStoreUploadFiles = o.R.HashStoreUploadFiles[:ln-1]
			break
		}
	}

	return nil
}

// StoreFileHashes retrieves all the records using an executor.
func StoreFileHashes(mods ...qm.QueryMod) storeFileHashQuery {
	mods = append(mods, qm.From("\"cloud_symbol_server\".\"store_file_hashes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"cloud_symbol_server\".\"store_file_hashes\".*"})
	}

	return storeFileHashQuery{q}
}

// FindStoreFileHash retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindStoreFileHash(ctx context.Context, exec boil.ContextExecutor, hashID int, selectCols ...string) (*StoreFileHash, error) {
	storeFileHashObj := &StoreFileHash{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"cloud_symbol_server\".\"store_file_hashes\" where \"hash_id\"=$1", sel,
	)

	q := queries.Raw(query, hashID)

	err := q.Bind(ctx, exec, storeFileHashObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from store_file_hashes")
	}

	if err = storeFileHashObj.doAfterSelectHooks(ctx, exec); err != nil {
		return storeFileHashObj, err
	}

	return storeFileHashObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *StoreFileHash) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no store_file_hashes provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(storeFileHashColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	storeFileHashInsertCacheMut.RLock()
	cache, cached := storeFileHashInsertCache[key]
	storeFileHashInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			storeFileHashAllColumns,
			storeFileHashColumnsWithDefault,
			storeFileHashColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, storeFileHashGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(storeFileHashType, storeFileHashMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(storeFileHashType, storeFileHashMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"cloud_symbol_server\".\"store_file_hashes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"cloud_symbol_server\".\"store_file_hashes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into store_file_hashes")
	}

	if !cached {
		storeFileHashInsertCacheMut.Lock()
		storeFileHashInsertCache[key] = cache
		storeFileHashInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the StoreFileHash.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *StoreFileHash) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	storeFileHashUpdateCacheMut.RLock()
	cache, cached := storeFileHashUpdateCache[key]
	storeFileHashUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			storeFileHashAllColumns,
			storeFileHashPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, storeFileHashGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update store_file_hashes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"cloud_symbol_server\".\"store_file_hashes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, storeFileHashPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(storeFileHashType, storeFileHashMapping, append(wl, storeFileHashPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update store_file_hashes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for store_file_hashes")
	}

	if !cached {
		storeFileHashUpdateCacheMut.Lock()
		storeFileHashUpdateCache[key] = cache
		storeFileHashUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q storeFileHashQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for store_file_hashes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for store_file_hashes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o StoreFileHashSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), storeFileHashPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"cloud_symbol_server\".\"store_file_hashes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, storeFileHashPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in storeFileHash slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all storeFileHash")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *StoreFileHash) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no store_file_hashes provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(storeFileHashColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	storeFileHashUpsertCacheMut.RLock()
	cache, cached := storeFileHashUpsertCache[key]
	storeFileHashUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			storeFileHashAllColumns,
			storeFileHashColumnsWithDefault,
			storeFileHashColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			storeFileHashAllColumns,
			storeFileHashPrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, storeFileHashGeneratedColumns)
		update = strmangle.SetComplement(update, storeFileHashGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert store_file_hashes, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(storeFileHashPrimaryKeyColumns))
			copy(conflict, storeFileHashPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"cloud_symbol_server\".\"store_file_hashes\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(storeFileHashType, storeFileHashMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(storeFileHashType, storeFileHashMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert store_file_hashes")
	}

	if !cached {
		storeFileHashUpsertCacheMut.Lock()
		storeFileHashUpsertCache[key] = cache
		storeFileHashUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single StoreFileHash record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *StoreFileHash) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no StoreFileHash provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), storeFileHashPrimaryKeyMapping)
	sql := "DELETE FROM \"cloud_symbol_server\".\"store_file_hashes\" WHERE \"hash_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from store_file_hashes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for store_file_hashes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q storeFileHashQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no storeFileHashQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from store_file_hashes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for store_file_hashes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o StoreFileHashSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(storeFileHashBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), storeFileHashPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"cloud_symbol_server\".\"store_file_hashes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, storeFileHashPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from storeFileHash slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for store_file_hashes")
	}

	if len(storeFileHashAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *StoreFileHash) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindStoreFileHash(ctx, exec, o.HashID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *StoreFileHashSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := StoreFileHashSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), storeFileHashPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"cloud_symbol_server\".\"store_file_hashes\".* FROM \"cloud_symbol_server\".\"store_file_hashes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, storeFileHashPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in StoreFileHashSlice")
	}

	*o = slice

	return nil
}

// StoreFileHashExists checks if the StoreFileHash row exists.
func StoreFileHashExists(ctx context.Context, exec boil.ContextExecutor, hashID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"cloud_symbol_server\".\"store_file_hashes\" where \"hash_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, hashID)
	}
	row := exec.QueryRowContext(ctx, sql, hashID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if store_file_hashes exists")
	}

	return exists, nil
}
